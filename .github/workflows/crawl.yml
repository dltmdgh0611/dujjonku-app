name: 🍪 두쫀쿠 재고 업데이트

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Install requests
        run: pip install requests
      
      - name: 🍪 크롤링 실행
        run: python crawler/crawl.py
      
      - name: 📤 Commit & Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "🍪 두쫀쿠봇"
          git add public/stores.json
          git diff --staged --quiet || git commit -m "🍪 재고 업데이트 $(TZ=Asia/Seoul date +'%m/%d %H:%M')"
          git push
